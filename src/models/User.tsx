export interface User {
    id: number,
    username: string,
    name: string,
    bio: string,
    profilePicture: string,
    followersCount: number,
    followingCount: number,
    certified: boolean
}